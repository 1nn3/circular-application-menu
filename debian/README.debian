Build-Depends
=============
For simplicity I set all required versions to the current available version in Debian.

Binary
======
The Circular-Main-Menu binary is installed as cmm(1). The circular-main-menu(1) command is just a link to cmm(1).
